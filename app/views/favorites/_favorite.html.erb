<% if book.favorited_by?(current_user) %>
  <p>
    <%= link_to '💖', book_favorites_path(book), method: :delete, remote:true %>
    <%= book.favorites.count %>
  </p>
  <% else %>
  <p>
    <%= link_to '🖤', book_favorites_path(book), method: :post, remote:true %>
    <%= book.favorites.count %>
  </p>
<% end %>
